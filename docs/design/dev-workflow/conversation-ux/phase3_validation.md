---
phase: 3
feature: conversation-ux
category: dev-workflow
status: complete
created: 2026-07-08
---

# Phase 3: Validation

## 1. TD 검토 결과 (🔍 Ecosystem Analyst, TD 역할)

### 🟢 낮음 — 기계적 반영으로 충분

| 항목 | 판단 |
|---|---|
| 박스 교체 ~20곳 | 확정 문면 치환 — G4 검증 방식(확정 문면 + 전후 대조) 재사용 |
| 추천 줄·ID 라벨 병기·요약 형식·페르소나 개행·그룹 이모지 | 서술 규범 추가 — 회귀 표면 없음 |
| HANDOFF 질문 진행 줄 | 신설 1줄 + 목록 — 기존 마커 무변경 |
| 부트스트랩 폴백 동기화 | 1줄 교체, 2.5KB 예산 내 |

### 🟡 중간 — 주의 필요 3건

| 항목 | 판단 | 가이드라인 |
|---|---|---|
| §4 응답 처리 재작성 | "일괄 수용"→"선택지 노출 항목만"은 의미 역전 — 구현 중 재작성 시 표류 위험 | **바뀐 문면을 설계 문서/plan에 완성형으로 사전 확정**, 구현은 verbatim 반영만 |
| 질문 트랙 + brainstorming 접합 | Step A 흐름 변경 | **정제(수집·병합·정렬)는 유지, 노출만 순차로** — 수집 로직 불변 |
| 로컬 화면 ~10개 개정 | 선택지 수·분기 의미 변형 시 회귀 | **게이트 대조표(화면 이름·선택지 수·0번 유무 전후 비교)** — G4 F2 패턴 재사용 → 리스크 낮음으로 강등 |

### 🔴 검증 불가 1건 (정직한 한계)

- "초심자 언어 기본값"은 기계 검증 불가 — 스켈레톤의 "고르면 어떻게 되는지" 슬롯이 구조로 절반을 강제하고, 본 세션의 실물 대화 예시를 문서에 수록해 모방 유도. 이후는 사용 관측으로 개선 (도그푸딩 관측 대상)

## 2. 재협의 항목

없음 — 전 요구사항 진행 가능 판정. 재논의 대기열 0건.

## 3. TD 기술 가이드라인

1. §4 재작성 문면과 신규 스켈레톤 예시는 **설계 문서 단계에서 완성형 확정** — 구현 태스크는 치환만 수행 (재작성 드리프트 방지)
2. 검증 체계는 G4 패턴 재사용: 게이트 대조 인벤토리(전후 화면 이름·선택지 수·0번 유무) + 확정 문면 grep — 별도 신규 검증 인프라 불필요
3. brainstorming 접합부: 통합 정제 출력 형식 펜스(references/templates.md)의 "일괄 목록" 부분만 "순차 노출 + ❓ N/M" 규칙으로 교체, 수집·병합·힌트 로직 무변경
4. decision-flow.md는 한 파일 유지 확정 — 내부 3부 구성(결정 규칙/질문 규칙/공통 형식·언어)으로 재편, §간 상호참조는 파일 내 §참조만 사용
5. 적용 범위 밖(REVIEW AC 박스, merge-to-domain·design-summary 출력, 산출물 내부 표, 디렉토리 트리)은 무변경 — 예외 목록을 문서에 명시해 오적용 방지

## 4. 페르소나 피드백 결과

- 국면 3은 TD 단독 검토(조사 보고서 기반) — 신규 쟁점 없음, 결정 요청 0건
- 세션 중 실시간 형식 규범 최종 확정 목록 (REQ-008에 수렴): 추천 줄 분리 / 구분 헤더+`-` 나열+빈 줄 / `/` 인라인 금지 / 페르소나 이름 줄+개행 / 그룹 헤더 이모지·구분선
